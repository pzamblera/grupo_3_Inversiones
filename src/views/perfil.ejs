<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/70073b8ae5.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/styles.css">
    <title>Perfil</title>
</head>
<body class="mainStyle">

  <%- include('partials/header') %>

    <section class="administrador">
        
        <div class="">
            <div class="container text-center">
                <div class="row justify-content-evenly">    
                    <img src="/img/<%=usuario.avatar%>" alt="" class="avatar col-sm-4">
                    
                    <div class="col-sm-8 fontSize row align-items-center nombreUsuario">
                        <p>Hola <%=usuario.nombre%></p>
                        <p>Disponible para invertir:</p>
                        <p>$<%=usuario.monto_billetera%></p>
                    </div>
                </div>
                <div class="row justify-content-evenly"> 
                    
                    <a class="col-8 col-md-2 botonPerfil" href="/ingresar"><button type="button" class="btn btn-success colorButton">Ingresar dinero</button></a> 
                    <a class="col-8 col-md-2 botonPerfil" href="/retirar"><button type="button" class="btn btn-success colorButton">Retirar dinero</button></a> 
                    <a class="col-8 col-md-2 botonPerfil" href="/invertir"><button type="button" class="btn btn-success colorButton">Invertir</button></a> 
                    <a class="col-8 col-md-2 botonPerfil" href="/carrito"><button type="button" class="btn btn-success colorButton">Carrito</button></a> 
                    <a class="col-8 col-md-2 botonPerfil" href="/vercarrito"><button type="button" class="btn btn-success colorButton">Ver carrito</button></a> 
                </div>
                <div class="historial"> 
                    <h2>Historial de movimientos</h2><br>
                <% for(let i=0; i<historial_movimiento.length; i++) { %>
                    <% if(historial_movimiento[i].id_movimiento == 3) { %>
                    <p>Tu inversión de $<%=historial_movimiento[i].monto_inicial%> tuvo un rendimiento de $<%=historial_movimiento[i].rendimiento%></p>                  
                    <% } if(historial_movimiento[i].id_movimiento == 2) { %> 
                    <p>Restiraste de tu cuenta $<%=historial_movimiento[i].monto_inicial%>.</p>
                    <% } if(historial_movimiento[i].id_movimiento == 1) { %>
                    <p>Ingresaste a tu cuenta $<%=historial_movimiento[i].monto_inicial%>.</p>
                    <% } %>
                    <% } %>
                </div>
            </div>
        </div>       
    
        
    </section>

      <%- include('partials/footer') %>

</body>
</html>